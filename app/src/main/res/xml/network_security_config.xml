<?xml version="1.0" encoding="utf-8"?>
<!-- Permite HTTP en claro SOLO a los hosts de desarrollo (Docker local).
     10.0.2.2 = localhost del Mac desde el emulador.
     192.168.1.225 = IP local del Mac para el móvil físico en la misma WiFi
     (cámbiala si tu IP local cambia). El resto de tráfico sigue obligando HTTPS. -->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">192.168.1.225</domain>
    </domain-config>
</network-security-config>
